<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.My_second_app" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.My_second_app.Splash" parent="Theme.My_second_app">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    
    <!-- Green theme -->
    <style name="Theme.Green" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/green_primary</item>
        <item name="android:windowBackground">@color/green_background</item>
    </style>
</resources>