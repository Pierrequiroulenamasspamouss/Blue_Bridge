package com.jowell.wellmonitoring.ui

/*
class WellConfigurationModel: ViewModel(){
    var WellData = mutableStateOf(WellData())
    fun onEvent(event: WellConfigEvents) {
        when (event) {
            is WellConfigEvents.WellNameEntered -> {
                WellData.value = WellData.value.copy(
                    wellName = event.wellName
                )
            }
            is WellConfigEvents.WellLocationEntered -> {
                WellData.value = WellData.value.copy(
                    wellLocation = event.wellLocation
                )
            }
            is WellConfigEvents.WellCapacityEntered -> {
                WellData.value = WellData.value.copy(
                    wellCapacity = event.wellCapacity
                )
            }
            is WellConfigEvents.WaterLevelEntered -> {
                WellData.value = WellData.value.copy(
                    wellWaterLevel = event.wellWaterLevel
                )
            }
            is WellConfigEvents.ConsumptionEntered -> {
                WellData.value = WellData.value.copy(
                    wellWaterConsumption = event.wellWaterConsumption
                )
            }
            is WellConfigEvents.WaterTypeEntered -> {
                WellData.value = WellData.value.copy(
                    wellWaterType = event.wellWaterType
                )
            }
            is WellConfigEvents.OwnerEntered -> {
                WellData.value = WellData.value.copy(
                    wellOwner = event.wellOwner
                )
            }
        }
    }
}

 */