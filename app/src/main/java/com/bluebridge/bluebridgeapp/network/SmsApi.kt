package com.bluebridge.bluebridgeapp.network

//TODO: here should be the backend to the SMS communication (like the sending functions, an interface with the custom API. )
// you have freedom of choice of how the calls should look like I didn't create the API yet. Just make the communication as efficient as possible,
// because SMS's are 160 characters, and no unicode. Basic requests to the server, like GetNearestWater ( alias GNW ), SendHelp(alias SH) + precise location that sends an urgent request with the user location, etc...
